# Burger Barn — Vercel package

This folder is ready to deploy as a static website on Vercel.

## Deploy

1. Unzip the package.
2. Add the folder to a Git repository.
3. Import that repository into Vercel.
4. Leave the framework preset as **Other** and deploy.

No build command, output directory, environment variables, or database are required.
